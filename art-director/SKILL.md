{"provided": "use `site-scraper` to pull key text + claims + FAQs.\n- If Pinterest: use `pinterest-assistant` workflow (links/screenshots) to extract vibe.\n\n### 2) Analyse (director-level)\n- art style / brush / texture\n- palette (primary/secondary/accent + banned)\n- vibes", "for": 1, "prompt": "duration 5 --ratio 9:16\n  bash scripts/kling-video.sh image2video", "url": "duration 5 --ratio 9:16\n  bash scripts/wan-video.sh image2video", "url\" --pro  # 1080p ($0.80)\n  bash scripts/wan-video.sh text2image \"prompt": "ratio 1:1\n  ```\n- **Kling AI 3.0** (Omni model) \u2014 text-to-video and image-to-video", "url\" --duration 5\n  ```\n- **Sora 2** \u2014 premium video generation (needs OPENAI_API_KEY)\n  ```bash\n  bash scripts/sora-video.sh generate \"prompt": "seconds 5 --size 720x1280\n  ```\n- **Remotion** \u2014 React-based programmatic video creation (captions", "multimodal)": "bash\nbash scripts/audit-visual.sh audit-image path/to/image.jpg\nbash scripts/audit-visual.sh audit-video path/to/video.mp4\nbash scripts/audit-visual.sh audit-batch path/to/directory/\n```\nScores: brand_fit", "Verdict": "pass / needs_revision / reject.\n\n### 7) Output Types\nUse the output type framework for different content formats:\n```bash\nbash scripts/output-types.sh list                              # Show all types\nbash scripts/output-types.sh specs broll                       # Get specs for B-roll\nbash scripts/output-types.sh generate promotion --prompt", " --kling  # Generate promotion video\nbash scripts/output-types.sh storyboard ugc --prompt \"...": "Generate UGC storyboard\n```\n\nSupported output types: broll", "Output": "scores (1-5 per dimension)\n- exactly what to change next (3 edits max)\n- Art Director uses Kimi K2.5 (multimodal) to visually review generated images\n- Auto-audit via `audit-visual.sh` for batch QA\n\n---\n\n## Tools & Scripts\n\n| Script | Purpose |\n|--------|---------|\n| `scripts/wan-video.sh` | Wan (Alibaba) video/image generation via fal.ai (text2video", "Wan/fal.ai**": "requires `FAL_API_KEY` in environment\n- **Kling**: stored in `~/.openclaw/workspace/ops/.kling-env` (source before use)\n- **Sora**: requires `OPENAI_API_KEY` in environment\n- **Kimi K2.5**: Moonshot API (configured in openclaw.json)\n\n## Inputs (minimum)\n- Brand: Gaia / Pinxin (or new)\n- Goal: what asset is for\n- Output type: broll / aroll / promotion / education / ugc / hero / carousel / etc.\n- Format: ratios + platform\n- Refs: links/screenshots\n- Exact text to place (if any)\n- Constraints: must include / avoid\n\n## Outputs (standard)\n1) Brand-fit **Style DNA**\n2) Prompt packs (NanoBanana + Z-Image + Midjourney)\n3) 3 controlled variants\n4) Video storyboard + generated clips (Kling 3.0 / Sora 2 / Remotion)\n5) Visual audit scores + next iteration notes\n6) Output type specs used\n\n## Content Factory Integration\n- All approved images \u2192 `seed-store.sh add --type image`\n- All approved videos \u2192 `seed-store.sh add --type video`\n- Storyboards \u2192 `seed-store.sh add --type storyboard`\n- Audit results \u2192 logged to `~/.openclaw/logs/visual-audit.log`\n- Output type data \u2192 `~/.openclaw/workspace/data/output-types.json`\n\n## Notes\n- Do not claim exact replication of any specific studio's copyrighted style; aim for **similar vibe** with original compositions.\n- Artee (Art Director) agent model: Kimi K2.5 (multimodal \u2014 can see and critique images)\n- Fallback for text-only tasks: GLM-4.7\n- Kling uses model `kling-video-o1` (Omni"}