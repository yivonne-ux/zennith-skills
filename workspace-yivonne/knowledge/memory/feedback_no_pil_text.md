---
name: No PIL text rendering — NANO does ALL text and numbers
description: User explicitly rejected PIL/Python text rendering. ALL text, numbers, copy in ads must be generated by NANO. PIL is ONLY for post-processing (resize 1080x1350, place_logo, add_grain). No exceptions.
type: feedback
---

## NO PIL TEXT RENDERING (2026-03-14)

User said: "NO PIL !!" and "all text numbers all nano."

**Why:** The user wants the organic, AI-native aesthetic that NANO produces. PIL-rendered text looks mechanical and breaks the authentic feel. The whole pipeline philosophy is NANO edit-first — PIL draws nothing.

**How to apply:**
- NANO generates ALL visible text: headlines, copy, numbers, prices, handwritten notes, UI text
- PIL does ONLY: `resize(1080,1350) → place_logo(auto) → add_grain(0.016)`
- For formats where text accuracy is critical (calculator, notes app), use better references + text_fix_prompt passes + fix rounds
- Accept iteration — CN ads v4 took 4 fix rounds. That's the process.
- NEVER propose PIL text overlay, PIL number rendering, or "hybrid PIL+NANO" approaches
